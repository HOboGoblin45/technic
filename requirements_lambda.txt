# Lambda-specific requirements - minimal dependencies for scanner
# This avoids the complex dependency conflicts

# Core dependencies
numpy==1.24.3
pandas==2.0.3
requests==2.31.0

# Data fetching
polygon-api-client==1.12.5

# Redis caching
redis==5.0.0

# AWS SDK (for Lambda integration)
boto3==1.34.0

# Technical analysis
ta-lib==0.4.28

# Optional: If you need these specific versions
scikit-learn==1.3.0
scipy==1.11.3

# Note: Many other dependencies from requirements.txt are not needed in Lambda
# Lambda has a 250MB unzipped package limit, so we keep it minimal
